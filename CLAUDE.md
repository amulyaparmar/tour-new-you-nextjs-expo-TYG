Read `AGENTS.md` at the repo root for project conventions and available agent skills
(Claude Code does not load AGENTS.md automatically). In particular, any request to
create, revise, or review a roleplay training scenario must follow the
`roleplay-scenario-author` skill (`.claude/skills/roleplay-scenario-author/` — a stub
that points at the canonical `.agents/skills/roleplay-scenario-author/SKILL.md`).
