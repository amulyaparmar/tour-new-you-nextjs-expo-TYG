// Keep the shorter public contact-card URL working alongside the canonical
// `/p/:property/:member` route used by generated links.
export { default, generateMetadata } from "../../p/[slug]/[member]/page";
